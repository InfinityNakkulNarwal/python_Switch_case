import switch_case              #import this package
list1 = ['a', 'b', 'c', 'd']    #define a list
a = switch_case.switch(list1)   #pass the list from this function
print(a)                        #the function will return the output, so use print or store it as a variable.